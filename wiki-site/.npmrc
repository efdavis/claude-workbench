registry=https://registry.npmjs.org/

# docusaurus-plugin-backlinks@1.0.7 declares a stale `react@^18` peer. It works
# fine on the React 19 / Docusaurus 3.10 stack this site uses, so accept the
# resolution instead of erroring on a fresh `npm install`.
legacy-peer-deps=true
