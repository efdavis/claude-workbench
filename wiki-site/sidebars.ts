import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
 * The sidebar is auto-generated from the articles directory (resolved in
 * docusaurus.config.ts from CLAUDE_WIKI_DIR, default ./example-wiki).
 *
 * Ordering: flat articles list alphabetically. To curate order/grouping,
 * either add `sidebar_position:` to article frontmatter or drop
 * `_category_.json` files into subfolders of your wiki's articles/ dir.
 * No slugs are hardcoded here, so the sidebar tracks whatever content you
 * point the site at.
 */
const sidebars: SidebarsConfig = {
  tutorialSidebar: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
